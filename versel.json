{
    "version": 2,
    "builds": [
        {
            "src": "./server/server.js",
            "use": "@versel/node"
        }
    ],

    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}